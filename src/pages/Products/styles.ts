import styled from 'styled-components';

export const Container = styled.div`
  span {
    color: inherit;
    margin: 0;
    font-size: 18px;
    font-weight: 300;
    line-height: 30px;
    letter-spacing: unset;
    text-transform: none;
  }
`;
